---
interface Props {
	title?: string;
}

const { title = "Mi sitio web" } = Astro.props;
import '../styles/global.css' 
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content="Mi sitio web" />

		<title>{title}</title>
	</head>

	<body>
		<slot />
	</body>
</html>